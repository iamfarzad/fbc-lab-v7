// Tool service schemas and types
export { WebcamCaptureSchema, ScreenShareSchema } from '../validation'

// Re-export for backward compatibility
export { WebcamCaptureSchema as default } from '../validation'